## Data Directory

This directory contains raw and minimally processed price data used in the analysis.

Raw data is never modified directly. All transformations are performed via scripts in the `src/` directory to ensure reproducibility.
